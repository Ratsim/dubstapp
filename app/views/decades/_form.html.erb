<%= form_with(model: decade, local: true) do |form| %>
  <% if decade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(decade.errors.count, "error") %> prohibited this decade from being saved:</h2>

      <ul>
      <% decade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :song %>
    <%= form.text_field :song %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
